
function 副本2_猫卡一图_脱离()
	显示('执行脱离模式')
	
	local 上次城镇点击时间 = 0
	
	local 最小城镇操作间隔 = 15000 -- 5秒
	
	local function 当前时间毫秒()
		return os.time() * 1000
	end
	初始化城镇界面()
	local 初始化页面 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		显示('猫卡脱离调整版本运行中...')
		-- 检测游戏是否闪退
		if 快速检测游戏状态() then
			
		end
		
		local 现在时间 = 当前时间毫秒()
		
		if 判断_城镇界面() then
			
			if 判断_疲劳值为0() then
				
				return
			end
			
			if 现在时间 - 上次城镇点击时间 > 最小城镇操作间隔 then
				显示('执行城镇界面操作')
				随机延时(500 , 1000)
				按下点击({1102 , 629 , 1154 , 679}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				随机延时(2000 , 3000)
				-- 更新最后操作时间
				
			else
				显示('城镇界面操作冷却中，跳过...')
				随机延时(1000 , 2000)
			end
			查找开始时间 = os . time()
			找到地图 = false
		elseif 判断_选择地图界面() then
			
			if not 初始化页面 then
				
				if 判断_洛兰之森() then
					初始化页面 = true
					
				else
					滑动(302 , 256 , 302 , 549 , 10 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
					
				end
				
			else
				if 入口_洛兰() then
					if 判断_已选中洛兰() then
						入口_区域移动()
						随机延时(400 , 1000)
					end
					查找开始时间 = os . time()
					找到地图 = true
				else
					if not 找到地图 then
						if os . time() - 查找开始时间 > 20 then
							
							return
						end
					end
					滑动(302 , 581 , 303 , 378 , 50 , 10 , {15 , 35} , {800 , 1000} , {100 , 200})
					随机延时(500 , 1500)
				end
				上次城镇点击时间 = 当前时间毫秒()
			end
			
		elseif 判断_选择地下城界面() then
			--显示('在选择地下城界面')
			随机延时(500 , 800)
			if 判断_洛兰地图() then
				按下点击({58 , 126 , 194 , 163}) -- 普通
				随机延时(500 , 1000)
				按下点击({318 , 409 , 479 , 471}) -- 封锁的试验场
				
			else
				
				清屏(true)
				return 副本2_猫卡一图_脱离()
			end
			上次城镇点击时间 = 当前时间毫秒()
		elseif 判断_可开燃烧交易界面() then
			
			if 判断_选择副本判断疲劳值为0() then
				
				初始化城镇界面2()
				return
			end
			
			--显示('选择副本难度界面')
			随机延时(800 , 1200)
			--显示('选择难度1')
			按下点击({110 , 247 , 385 , 325}) -- 选择1
			同步副本2燃烧疲劳开关状态()
			
			随机延时(1000 , 1600)
			按下点击({1060 , 612 , 1179 , 660}) -- 开始战斗
			随机延时(800 , 1500)
			入口_确认()
			break
		elseif 判断_副本中() then
			
			自动刷猫图()
			判断_后跳_绿色()
			功能_脱离模式()
			猫卡一图回到城镇()
			break
		elseif 入口_确认(true) then
			
		elseif 判断_返回城镇() then
			功能_返回城镇界面获取疲劳值()
			入口_返回城镇()
			
		else
			if not 清屏(true) then
				上次城镇点击时间 = 当前时间毫秒()
			end
			
			显示('猫卡脱离模式进入副本中...')
			
		end
	end
	
	local 加载开始时间 = nil
	local 上一次状态 = nil
	
	local 猫图开始时间 = os.time()
	local 循环开始时间 = os.time()
	
	local function 格式化用时(开始时间)
		local 总用时 = os.time() - 开始时间
		local 小时 = math.floor(总用时 / 3600)
		local 分钟 = math.floor((总用时 % 3600) / 60)
		local 秒 = 总用时 % 60
		return string.format("%d小时%d分%d秒" , 小时 , 分钟 , 秒) , 总用时
	end
	local 清屏时间 = os.time()
	while true do
		随机延时(500 , 600)
		-- 检测游戏是否闪退
		if 快速检测游戏状态() or 判断_返回城镇_主城() then
			
			加载开始时间 = nil
			return 副本2_猫卡一图_脱离()
		end
		显示('猫卡免技能调整版本运行中...')
		if 判断_城镇界面() then
			显示('已返回城镇')
			
			加载开始时间 = nil -- 离开加载状态，重置
			上一次状态 = "城镇"
			
			if 判断_疲劳值为0() then
				初始化城镇界面()
				local 用时文本 , 总用时秒 = 格式化用时(循环开始时间)
				local 猫卡数量 = 功能_获取猫卡数量()
				
				if 猫卡数量 then
					print('副本2猫图完成!用时: ' .. 用时文本.. '猫卡总数: ' .. 猫卡数量)
					--发送喵提醒_自定义('猫图完成!用时: ' .. 用时文本 )
					发送喵提醒_自定义('猫图用时: ' .. 用时文本.. '猫卡总数: ' .. 猫卡数量)
				else
					print('副本2猫图完成!用时: ' .. 用时文本.. ' (未能识别猫卡数量)')
					发送喵提醒_自定义('猫图用时: ' .. 用时文本.. ' (未能识别猫卡数量)')
				end
				return
			end
			
			进副本移动模式()
			
			for i = 1 , 5 do
				if not 判断_城镇界面() then
					break
				else
					进副本移动模式()
				end
				
			end
			-- 如果连续10次检测到还在城镇界面，重新执行副本_猫卡一图
			if 判断_城镇界面() then
				return 副本2_猫卡一图_脱离()
			end
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 清屏_系统关闭X() then
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 判断_选择地下城界面() then
			加载开始时间 = nil
			上一次状态 = "选择地下城"
			if 判断_洛兰地图() then
				按下点击({58 , 126 , 194 , 163}) -- 普通
				随机延时(500 , 1000)
				按下点击({318 , 409 , 479 , 471}) -- 封锁的试验场
				
			else
				
				清屏(true)
				return 副本2_猫卡一图_脱离()
			end
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 判断_可开燃烧交易界面() then
			加载开始时间 = nil
			上一次状态 = "可开燃烧"
			if 判断_选择副本判断疲劳值为0() then
				初始化城镇界面()
				local 用时文本 , 总用时秒 = 格式化用时(循环开始时间)
				local 猫卡数量 = 功能_获取猫卡数量()
				
				if 猫卡数量 then
					print('副本2猫图完成!用时: ' .. 用时文本.. '猫卡总数: ' .. 猫卡数量)
					--发送喵提醒_自定义('猫图完成!用时: ' .. 用时文本 )
					发送喵提醒_自定义('猫图用时: ' .. 用时文本.. '猫卡总数: ' .. 猫卡数量)
				else
					print('副本2猫图完成!用时: ' .. 用时文本.. ' (未能识别猫卡数量)')
					发送喵提醒_自定义('猫图用时: ' .. 用时文本.. ' (未能识别猫卡数量)')
				end
				
				return
			end
			随机延时(300 , 400)
			按下点击({141 , 257 , 359 , 313})
			随机延时(300 , 400)
			按下点击({141 , 257 , 359 , 313})
			随机延时(600 , 800)
			按下点击({1060 , 612 , 1179 , 660}) -- 开始战斗
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 判断_加载界面() then
			sleep(1000)
			while true do
				
				if 检测游戏是否闪退() then
					break
				end
				if 判断_副本中_猫图() then
					加载开始时间 = nil
					上一次状态 = "副本"
					上一次状态 = "副本"
					
					自动刷猫图()
					判断_后跳_绿色()
					功能_脱离模式()
					猫卡一图回到城镇()
					break
				elseif 判断_城镇界面() or 判断_可开燃烧交易界面() then
					break
				end
				sleep(100)
			end
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 判断_副本中_猫图() then
			加载开始时间 = nil
			上一次状态 = "副本"
			上一次状态 = "副本"
			
			自动刷猫图()
			功能_脱离模式()
			判断_后跳_绿色()
			猫卡一图回到城镇()
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 入口_确认(true) then
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 判断_返回城镇() then
			功能_返回城镇界面获取疲劳值()
			入口_返回城镇()
			清屏时间 = os.time()
		else
			if os.time() - 清屏时间 > 1 then
				清屏(true)
			end
			-- 游戏加载中的处理
			local 当前时间 = os.time()
			-- 初始化加载开始时间（如果尚未初始化）
			if 加载开始时间 == nil then
				加载开始时间 = 当前时间
				--显示('开始记录游戏加载时间...')
			end
			
			-- 计算已加载时间
			local 已加载时间 = 当前时间 - 加载开始时间
			显示('猫卡脱离模式游戏加载中..... ' .. 已加载时间 .. '秒')
			
			-- 如果加载超过1分钟（30秒），执行清屏操作
			if 已加载时间 >= 10 then
				print('游戏加载超过30秒，执行清屏操作')
				if 清屏(true) then
					
					随机延时(400 , 600)
				end
				
			end
		end
	end
	
end

function 副本2_猫卡一图()--只捡猫卡
	显示('执行副本2极速版')
	初始化城镇界面()
	local 上次城镇点击时间 = 0
	
	local 最小城镇操作间隔 = 15000 -- 5秒
	local 初始化页面 = false
	-- 获取当前时间的函数
	local function 当前时间毫秒()
		return os.time() * 1000 -- 简单转换为毫秒单位
	end
	
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		显示('副本2 猫卡极速版 运行中...')
		-- 检测游戏是否闪退
		
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，继续执行副本功能')
			
		end
		
		local 现在时间 = 当前时间毫秒()
		
		if 判断_城镇界面() then
			-- 检查是否已经过了足够的间隔时间
			if 判断_疲劳值为0() then
				
				return
			end
			if 现在时间 - 上次城镇点击时间 > 最小城镇操作间隔 then
				显示('执行城镇界面操作')
				随机延时(500 , 1000)
				按下点击({1097 , 627 , 1159 , 674}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				随机延时(2000 , 3000)
				
			else
				显示('副本2_猫卡一图:城镇界面操作冷却中，跳过...')
				随机延时(1000 , 2000)
			end
			查找开始时间 = os . time()
			找到地图 = false
		elseif 判断_选择地图界面() then
			
			if not 初始化页面 then
				if 判断_洛兰之森() then
					初始化页面 = true
				else
					滑动(302 , 256 , 302 , 549 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				end
				
			else
				
				if 入口_洛兰() then
					if 判断_已选中洛兰() then
						入口_区域移动()
						随机延时(400 , 1000)
					end
					查找开始时间 = os . time()
					找到地图 = true
					上次城镇点击时间 = 当前时间毫秒()
				else
					if not 找到地图 then
						
						if os . time() - 查找开始时间 > 20 then
							
							return
							
						end
					end
					滑动(302 , 581 , 303 , 378 , 50 , 10 , {15 , 35} , {800 , 1000} , {100 , 200})
					随机延时(500 , 1500)
				end
			end
			
		elseif 判断_选择地下城界面() then
			-- 更新最后操作时间
			上次城镇点击时间 = 当前时间毫秒()
			--显示('在选择地下城界面')
			随机延时(500 , 800)
			if 判断_洛兰地图() then
				按下点击({58 , 126 , 194 , 163}) -- 普通
				随机延时(500 , 1000)
				按下点击({318 , 409 , 479 , 471}) -- 封锁的试验场
				
			else
				
				清屏(true)
				return 副本2_猫卡一图()
			end
			
		elseif 判断_可开燃烧交易界面() then
			随机延时(800 , 1200)
			
			if 判断_选择副本判断疲劳值为0() then
				
				初始化城镇界面()
				return
			end
			同步副本2燃烧疲劳开关状态()
			随机延时(400 , 500)
			按下点击({110 , 247 , 385 , 325}) -- 选择1
			随机延时(1500 , 2000)
			
			按下点击({1060 , 612 , 1179 , 660}) -- 开始战斗
			随机延时(800 , 1500)
			入口_确认()
			break
			
		elseif 判断_副本中() then
			
			自动刷猫图()
			yolo自动捡猫卡()
			猫卡一图回到城镇()
			break
		elseif 入口_确认(true) then
			
		elseif 判断_返回城镇() then
			功能_返回城镇界面获取疲劳值()
			入口_返回城镇()
			
		else
			
			-- 安全清屏机制：避免界面异常时卡死
			if 清屏(true) then
				print('执行清屏操作，避免界面卡死')
				随机延时(500 , 1000)
			else
				上次城镇点击时间 = 当前时间毫秒()
			end
		end
	end
	local 加载开始时间 = nil
	local 上一次状态 = nil
	local 循环开始时间 = os.time()
	
	local function 格式化用时(开始时间)
		local 总用时 = os.time() - 开始时间
		local 小时 = math.floor(总用时 / 3600)
		local 分钟 = math.floor((总用时 % 3600) / 60)
		local 秒 = 总用时 % 60
		return string.format("%d小时%d分%d秒" , 小时 , 分钟 , 秒) , 总用时
	end
	local 清屏时间 = os.time()
	while true do
		sleep(500)
		显示('副本2 猫卡极速版2 运行中...')
		-- 检测游戏是否闪退
		if 快速检测游戏状态() or 判断_返回城镇_主城() then
			
			加载开始时间 = nil
			return 副本2_猫卡一图()
		end
		
		if 判断_城镇界面() then
			
			显示('已返回城镇')
			
			加载开始时间 = nil -- 离开加载状态，重置
			上一次状态 = "城镇"
			if 判断_疲劳值为0() then
				初始化城镇界面()
				local 用时文本 , 总用时秒 = 格式化用时(循环开始时间)
				
				local 猫卡数量 = 功能_获取猫卡数量()
				
				if 猫卡数量 then
					print('副本2猫图完成!用时: ' .. 用时文本.. '猫卡总数: ' .. 猫卡数量)
					--发送喵提醒_自定义('猫图完成!用时: ' .. 用时文本 )
					发送喵提醒_自定义('猫图用时: ' .. 用时文本.. '猫卡总数: ' .. 猫卡数量)
				else
					print('副本2猫图完成!用时: ' .. 用时文本.. ' (未能识别猫卡数量)')
					发送喵提醒_自定义('猫图用时: ' .. 用时文本.. ' (未能识别猫卡数量)')
				end
				return
			end
			
			进副本移动模式()
			
			for i = 1 , 10 do
				if not 判断_城镇界面() then
					break
				else
					进副本移动模式()
				end
				sleep(200)
			end
			
			-- 如果连续10次检测到还在城镇界面，重新执行副本2_猫卡一图
			if 判断_城镇界面() then
				
				显示('连续10次检测到还在城镇界面，重新执行副本2_猫卡一图')
				
				return 副本2_猫卡一图()
			end
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 清屏_系统关闭X() then
			随机延时(500 , 800)
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 判断_选择地下城界面() then
			加载开始时间 = nil
			上一次状态 = "选择地下城"
			随机延时(500 , 800)
			if 判断_洛兰地图() then
				
				按下点击({58 , 126 , 194 , 163}) -- 普通
				随机延时(500 , 1000)
				按下点击({318 , 409 , 479 , 471}) -- 封锁的试验场
				
			else
				
				清屏(true)
				return 副本2_猫卡一图()
			end
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 判断_可开燃烧交易界面() then
			加载开始时间 = nil
			上一次状态 = "可开燃烧"
			if 判断_选择副本判断疲劳值为0() then
				初始化城镇界面()
				local 用时文本 , 总用时秒 = 格式化用时(循环开始时间)
				
				local 猫卡数量 = 功能_获取猫卡数量()
				
				if 猫卡数量 then
					print('副本2猫图完成!用时: ' .. 用时文本.. '猫卡总数: ' .. 猫卡数量)
					--发送喵提醒_自定义('猫图完成!用时: ' .. 用时文本 )
					发送喵提醒_自定义('猫图用时: ' .. 用时文本.. '猫卡总数: ' .. 猫卡数量)
				else
					print('副本2猫图完成!用时: ' .. 用时文本.. ' (未能识别猫卡数量)')
					发送喵提醒_自定义('猫图用时: ' .. 用时文本.. ' (未能识别猫卡数量)')
				end
				return
			end
			随机延时(300 , 400)
			按下点击({141 , 257 , 359 , 313})
			随机延时(300 , 400)
			按下点击({141 , 257 , 359 , 313})
			随机延时(600 , 800)
			按下点击({1060 , 612 , 1179 , 660}) -- 开始战斗
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 判断_加载界面() then
			sleep(1000)
			while true do
				if 检测游戏是否闪退() then
					break
				end
				
				if 判断_副本中_猫图() then
					加载开始时间 = nil
					上一次状态 = "副本"
					上一次状态 = "副本"
					
					自动刷猫图()
					yolo自动捡猫卡()
					--yolo自动捡猫卡单手模式()
					猫卡一图回到城镇()
					break
				elseif 判断_城镇界面() or 判断_可开燃烧交易界面() then
					break
				end
				sleep(100)
			end
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 判断_副本中_猫图() then
			加载开始时间 = nil
			上一次状态 = "副本"
			上一次状态 = "副本"
			
			自动刷猫图()
			yolo自动捡猫卡()
			猫卡一图回到城镇()
			加载开始时间 = nil
			清屏时间 = os.time()
		elseif 判断_返回城镇() then
			功能_返回城镇界面获取疲劳值()
			入口_返回城镇()
			清屏时间 = os.time()
		elseif 入口_确认(true) then
			随机延时(600 , 800)
			加载开始时间 = nil
			清屏时间 = os.time()
		else
			if os.time() - 清屏时间 > 1 then
				清屏(true)
			end
			-- 游戏加载中的处理
			local 当前时间 = os.time()
			-- 初始化加载开始时间（如果尚未初始化）
			if 加载开始时间 == nil then
				加载开始时间 = 当前时间
				--显示('开始记录游戏加载时间...')
			end
			
			-- 计算已加载时间
			local 已加载时间 = 当前时间 - 加载开始时间
			显示('游戏加载中..... ' .. 已加载时间 .. '秒')
			-- 如果加载超过1分钟（60秒），执行清屏操作
			if 已加载时间 >= 60 then
				显示('游戏加载超过1分钟，执行强制重启操作副本2_猫卡一图')
				关闭(地下城)
				加载开始时间 = os.time() -- 重置加载时间
				
			end
			
		end
	end
end

function 副本2_深渊()
	print('执行副本2_深渊')
	初始化城镇界面()
	local 上次城镇判断时间 = 0 -- 新增：记录上次判断城镇的时间
	
	local 上次城镇点击时间 = 0 -- 新增：记录上次城镇点击的时间
	local 刚从副本中出来 = false
	local 当前疲劳值 = 功能_获取疲劳值()
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		显示('副本2_深渊运行中...')
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，继续执行副本功能')
			
			-- 不退出，继续执行副本功能
		end
		
		local 当前时间 = os.time()
		
		if 判断_城镇界面() then
			-- 离开难度选择界面，清除120秒计时
			
			local 当前疲劳值 = 功能_获取疲劳值()
			
			上次城镇判断时间 = 当前时间 -- 更新判断时间
			
			if 当前疲劳值 == 0 then
				
				return
			elseif 当前疲劳值 <= 4 then
				
				return 副本2_猫卡一图()
			end
			
			-- 检查是否已经过了足够的间隔时间（10秒）
			if 当前时间 - 上次城镇点击时间 > 20 or 刚从副本中出来 then
				
				if not 判断_委托_城镇入口() then
					
					return
				end
				判断_委托界面超时()
				刚从副本中出来 = false
			else
				显示('副本2_深渊:城镇界面操作冷却中，跳过...')
				
			end
		elseif 判断_委托界面() then
			
			if 判断_日常地下城() then
				
			elseif 判断_当前委托() then
				
			elseif 入口_深渊() then
				
			else
				return
				显示('未找到深渊结束副本2_深渊')
			end
			
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
		elseif 判断_深渊难度界面()then
			
			if 判断_委托不可入场() then
				--随机延时(200 , 800)
				清屏(true)
				return
				
			else
				按下点击({1094 , 646 , 1229 , 683}) --入场
				随机延时(2000 , 2500)
			end
			
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
		elseif 判断_副本中() then
			
			--随机释放技能()
			自动刷图()
			刚从副本中出来 = true
			
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			返回(4)
		elseif 判断_返回城镇() then
			print('副本2返回城镇界面')
			
			功能_返回城镇界面获取疲劳值()
			local 返回城镇开始时间 = os.time() -- 在此初始化计时器0
			print('副本2返回城镇界面,检查预留疲劳值')
			if 功能_获取疲劳值() == 0 or 功能_获取疲劳值() <= 4 then
				显示('到达预留疲劳值返回城镇再次确认')
				
				入口_返回城镇()
				初始化城镇界面()
			else
				
				显示('未到达预留疲劳值再次挑战')
				
				if 入口_再次挑战() then
					print('再次挑战')
					
				else
					
					print('*************************没检测到再次挑战返回城镇*************************')
					入口_返回城镇()
					初始化城镇界面()
				end
				
			end
			
		elseif 入口_确认(true) then
			
		else
			
			显示('副本2_深渊:加载中...')
			
			-- 安全清屏机制：避免界面异常时卡死
			if 清屏(true) then
				
			else
				上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			end
		end
		
	end
	
end
local function 判断_黑色大地不可入场()
	setDict(0 , "界面判断.txt")
	useDict(0)
	local ret6 , x6 , y6 = findStr(800 , 500 , 1279 , 719 , "不可入场" , "000000-303030" , 0.8 , 10 , 10)
	if x6 ~= - 1 and y6 ~= - 1 then--灰色入场标识
		显示('不可入场6')
		return true
	end
	setDict(0 , "界面判断.txt")
	useDict(0)
	local ret7 , x7 , y7 = findStr(813 , 195 , 975 , 251 , "大地抗魔值不足" , "ff0000-303030" , 0.8 , 10 , 10)
	if x7 ~= - 1 and y7 ~= - 1 then
		显示('大地抗魔值不足')
		return true
	end
	return false
end
function 副本2_黑色大地()--副本2
	print('执行副本2_黑色大地()')
	初始化城镇界面()
	local 上次城镇判断时间 = 0 -- 新增：记录上次判断城镇的时间
	
	local 上次城镇点击时间 = 0 -- 新增：记录上次城镇点击的时间
	local 刚从副本中出来 = false
	local 查找副本开始时间 = os.time()
	local 找到副本 = false
	local 首次运行 = true
	local 可入场 = false
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		显示('副本2 黑色大地 运行中...')
		local 当前时间 = os.time()
		
		-- 检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，继续执行副本功能')
			
			-- 不退出，继续执行副本功能
		end
		
		if 判断_城镇界面() then
			
			上次城镇判断时间 = 当前时间 -- 更新判断时间
			if 判断_疲劳值为0() then
				显示('没有疲劳值了')
				return
			end
			-- 检查是否已经过了足够的间隔时间（20秒）
			if 当前时间 - 上次城镇点击时间 > 20 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				按下点击({1202 , 443 , 1230 , 489})--委托
				判断_委托界面超时()
				刚从副本中出来 = false
			else
				显示('副本2_黑色大地:城镇界面操作冷却中，跳过...')
				
				随机延时(1000 , 2000)
			end
			可入场 = false
		elseif 判断_委托界面() then
			可入场 = false
			if 判断_日常地下城() then
				随机延时(500 , 600)
				
			else
				if 入口_黑色大地() then
					
					找到副本 = true
				else
					return
				end
			end
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
		elseif 判断_黑色大地选择难度界面() then
			
			if 判断_黑色大地不可入场() then
				
				初始化城镇界面()
				return
			end
			
			if 同步副本2燃烧疲劳开关状态() then
				显示('设置燃烧疲劳')
				随机延时(300 , 500)
			end
			按下点击({1097 , 635 , 1256 , 685})--入场
			随机延时(3000 , 3500)
			
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
		elseif 判断_副本中() then
			找到副本 = true
			自动刷图()
			刚从副本中出来 = true
			可入场 = false
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
		elseif 判断_返回城镇() then
			-- 在返回城镇界面检测疲劳值，必须识别成功
			local 当前疲劳值 = 功能_获取疲劳值()
			
			功能_返回城镇界面获取疲劳值()
			
			if 判断_副本中疲劳值为0() then
				
				入口_返回城镇()
				
				初始化城镇界面()
			else
				if 入口_再次挑战() then
					
					随机延时(800 , 1500)
					
				else
					入口_返回城镇()
					
					初始化城镇界面()
				end
				
			end
		elseif 入口_确认(true) then
			可入场 = false
		else
			
			显示('副本2_黑色大地:加载中...')
			-- 安全清屏机制：避免界面异常时卡死
			if 清屏(true) then
				print('执行清屏操作，避免界面卡死')
				随机延时(300 , 400)
			else
				上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			end
		end
	end
end
function 副本2_根防御战()
	初始化城镇界面()
	--local 燃烧ID = "checkbox1_角色" .. 角色序号 .. "_开燃烧疲劳"
	local 上次城镇判断时间 = 0 -- 新增：记录上次判断城镇的时间
	local 上次城镇点击时间 = 0 -- 新增：记录上次城镇点击的时间
	local 刚从副本中出来 = false
	local 初始化界面 = false
	local 等待燃烧界面加载 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		显示('副本2 根特 运行中...')
		local 当前时间 = os.time()
		
		-- 检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			
			上次城镇判断时间 = 当前时间 -- 更新判断时间
			
			if 判断_疲劳值为0() then
				
				return
			end
			-- 检查是否已经过了足够的间隔时间（10秒）
			if 当前时间 - 上次城镇点击时间 > 10 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				刚从副本中出来 = false
				
			else
				显示('副本2_根特:城镇界面操作冷却中，跳过...')
				
			end
			查找开始时间 = os . time()
			找到地图 = false
		elseif 判断_选择地图界面() then
			--显示('选择地图')
			随机延时(500 , 600)
			
			if not 初始化页面 then
				if 判断_洛兰之森() then
					初始化页面 = true
				else
					滑动(302 , 256 , 302 , 549 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				end
				
			else
				if 入口_安特贝鲁() then
					随机延时(500 , 600)
					
					if 判断_安特贝鲁_已选中() then
						入口_区域移动()
						
					end
					查找开始时间 = os . time()
					找到地图 = true
				else
					
					if not 找到地图 then
						if os . time() - 查找开始时间 > 20then
							
							return
						end
					end
					
					滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {100 , 200})
					随机延时(800 , 1000)
					
				end
				上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			end
			
		elseif 判断_选择地下城界面() then
			--显示('在选择地下城界面')
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			随机延时(500 , 600)
			按下点击({786 , 100 , 925 , 157}) -- 根特
			
		elseif 判断_可开燃烧交易界面() then
			
			if 判断_选择副本判断疲劳值为0() then
				
				清屏(true)
				
				return
			end
			同步副本2燃烧疲劳开关状态()
			随机延时(300 , 500)
			按下点击({1060 , 612 , 1179 , 660}) -- 开始战斗
			等待燃烧界面加载 = false
		elseif 判断_加载界面() then
			
		elseif 判断_副本中() then
			等待燃烧界面加载 = false
			自动刷图()
			刚从副本中出来 = true
			
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			随机延时(500 , 1000)
		elseif 判断_返回城镇() then
			
			功能_返回城镇界面获取疲劳值()
			
			if 判断_副本中疲劳值为0() then
				显示('疲劳值为0，返回城镇')
				入口_返回城镇()
				初始化城镇界面()
			else
				if 入口_再次挑战() then
					
				else
					初始化城镇界面()
					
				end
				
			end
		elseif 入口_确认(true) then
			
		else
			
			显示('副本2_根特:游戏加载中...')
			
			-- 安全清屏机制：避免界面异常时卡死
			if not 等待燃烧界面加载 then
				if 清屏(true) then
					print('执行清屏操作，避免界面卡死')
					
				end
			end
			
		end
	end
	
end
function 副本2_卡勒特司令部()
	初始化城镇界面()
	--local 燃烧ID = "checkbox1_角色" .. 角色序号 .. "_开燃烧疲劳"
	local 上次城镇判断时间 = 0 -- 新增：记录上次判断城镇的时间
	local 上次城镇点击时间 = 0 -- 新增：记录上次城镇点击的时间
	local 刚从副本中出来 = false
	local 初始化页面 = false
	local 等待燃烧界面加载 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		显示('副本2 卡勒特司令部 运行中...')
		local 当前时间 = os.time()
		
		-- 检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			
			上次城镇判断时间 = 当前时间 -- 更新判断时间
			
			if 判断_疲劳值为0() then
				
				return
			end
			-- 检查是否已经过了足够的间隔时间（10秒）
			if 当前时间 - 上次城镇点击时间 > 10 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				刚从副本中出来 = false
				
			else
				显示('副本2_卡勒特司令部:城镇界面操作冷却中，跳过...')
				
			end
			查找开始时间 = os . time()
			找到地图 = false
		elseif 判断_选择地图界面() then
			--显示('选择地图')
			if not 初始化页面 then
				if 判断_洛兰之森() then
					初始化页面 = true
				else
					滑动(302 , 256 , 302 , 549 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				end
				
			else
				if 入口_无法地带() then
					随机延时(500 , 600)
					if 判断_已选中无法地带() then
						入口_区域移动()
					end
					
					查找开始时间 = os . time()
					找到地图 = true
					上次城镇点击时间 = 当前时间 -- 更新最后点击时间
				else
					
					if not 找到地图 then
						if os . time() - 查找开始时间 > 20 then
							return
						end
					end
					
					滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {100 , 200})
					随机延时(800 , 1000)
					
				end
				
			end
			
		elseif 判断_选择地下城界面() then
			--显示('在选择地下城界面')
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			随机延时(500 , 1000)
			按下点击({833 , 292 , 954 , 345}) -- 英雄冢
			
		elseif 判断_可开燃烧交易界面() then
			
			if 判断_选择副本判断疲劳值为0() or 判断_副本不可入场() then
				
				清屏(true)
				
				return
			end
			
			同步副本2燃烧疲劳开关状态()
			随机延时(300 , 500)
			按下点击({1060 , 612 , 1179 , 660}) -- 开始战斗
			等待燃烧界面加载 = false
		elseif 判断_加载界面() then
			刚从副本中出来 = false
		elseif 判断_副本中() then
			自动刷图()
			刚从副本中出来 = true
			等待燃烧界面加载 = false
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			随机延时(500 , 1000)
		elseif 判断_返回城镇() then
			
			功能_返回城镇界面获取疲劳值()
			
			if 判断_副本中疲劳值为0() then
				显示('疲劳值为0，返回城镇')
				入口_返回城镇()
				
				初始化城镇界面()
			else
				
				if 入口_再次挑战() then
					
				else
					print('-----没找到再次挑战,返回城镇----')
					初始化城镇界面()
					
				end
				
			end
		elseif 入口_确认(true) then
			
		else
			
			显示('副本2_卡勒特司令部:游戏加载中...')
			
			-- 安全清屏机制：避免界面异常时卡死
			if not 等待燃烧界面加载 then
				if 清屏(true) then
					print('执行清屏操作，避免界面卡死')
				end
			end
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
		end
	end
	
end

function 副本2_海贼2图()
	初始化城镇界面()
	--local 燃烧ID = "checkbox1_角色" .. 角色序号 .. "_开燃烧疲劳"
	local 上次城镇判断时间 = 0 -- 新增：记录上次判断城镇的时间
	local 上次城镇点击时间 = 0 -- 新增：记录上次城镇点击的时间
	local 刚从副本中出来 = false
	local 初始化页面 = false
	local 等待燃烧界面加载 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		显示('副本2 海贼2图 运行中...')
		local 当前时间 = os.time()
		
		-- 检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			上次城镇判断时间 = 当前时间 -- 更新判断时间
			if 判断_疲劳值为0() then
				
				return
			end
			-- 检查是否已经过了足够的间隔时间（10秒）
			if 当前时间 - 上次城镇点击时间 > 20 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				随机延时(500 , 1000)
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				随机延时(2000 , 3000)
				刚从副本中出来 = false
				
			else
				显示('副本2_海贼2图:城镇界面操作冷却中，跳过...')
				随机延时(1000 , 2000)
			end
			查找开始时间 = os.time()
			找到地图 = false
		elseif 判断_选择地图界面() then
			--显示('选择地图')
			if not 初始化页面 then
				if 判断_洛兰之森() then
					初始化页面 = true
				else
					滑动(302 , 256 , 302 , 549 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				end
				
			else
				if 判断_海上列车() then
					
					随机延时(500 , 600)
					if 判断_已选中海上列车() then
						入口_区域移动()
					end
					查找开始时间 = os . time()
					找到地图 = true
					上次城镇点击时间 = 当前时间 -- 更
					
				else
					if not 找到地图 then
						if os . time() - 查找开始时间 > 20 then
							return
						end
					end
					滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {100 , 200})
					随机延时(500 , 1500)
				end
				
			end
			
		elseif 判断_选择地下城界面() then
			--显示('在选择地下城界面')
			随机延时(500 , 1000)
			按下点击({800 , 418 , 928 , 466}) -- 列车上的海贼
			
		elseif 判断_可开燃烧交易界面() then
			随机延时(400 , 500)
			if 判断_选择副本判断疲劳值为0() or 判断_副本不可入场() then
				
				清屏(true)
				
				return
			end
			同步副本2燃烧疲劳开关状态()
			随机延时(300 , 400)
			按下点击({105 , 256 , 387 , 331})--难度2
			随机延时(800 , 1500)
			按下点击({1060 , 612 , 1179 , 660}) -- 开始战斗
			等待燃烧界面加载 = false
		elseif 入口_确认() then
			sleep(2000)
		elseif 判断_加载界面() then
			刚从副本中出来 = false
		elseif 判断_副本中() then
			自动刷图()
			等待燃烧界面加载 = false
			刚从副本中出来 = true
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			随机延时(500 , 1000)
		elseif 判断_返回城镇() then
			
			功能_返回城镇界面获取疲劳值()
			
			if 判断_副本中疲劳值为0() then
				
				入口_返回城镇()
				
				初始化城镇界面()
				
			else
				
				if 入口_再次挑战() then
					
				else
					入口_返回城镇()
					
				end
			end
		elseif 入口_确认(true) then
		else
			显示('副本2_海贼2图:游戏加载中...')
			-- 安全清屏机制：避免界面异常时卡死
			if not 等待燃烧界面加载 then
				if 清屏(true) then
					print('执行清屏操作，避免界面卡死')
					随机延时(500 , 1000)
					
				end
			end
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			
		end
		
	end
end

function 副本2_阿登高地勇士()
	初始化城镇界面()
	--local 燃烧ID = "checkbox1_角色" .. 角色序号 .. "_开燃烧疲劳"
	local 上次城镇判断时间 = 0 -- 新增：记录上次判断城镇的时间
	local 上次城镇点击时间 = 0 -- 新增：记录上次城镇点击的时间
	local 刚从副本中出来 = false
	local 初始化页面 = false
	local 等待燃烧界面加载 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		显示('副本2 阿登高地勇士 运行中...')
		local 当前时间 = os.time()
		
		-- 检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			上次城镇判断时间 = 当前时间 -- 更新判断时间
			local 当前疲劳值 = 功能_获取疲劳值()
			
			if 当前疲劳值 <= 19 then
				
				print('疲劳值不足')
				
				return
			end
			
			-- 检查是否已经过了足够的间隔时间（10秒）
			if 当前时间 - 上次城镇点击时间 > 20 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				随机延时(500 , 1000)
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				刚从副本中出来 = false
				
			else
				显示('副本2_阿登高地勇士:城镇界面操作冷却中，跳过...')
				
				随机延时(1000 , 2000)
			end
			查找开始时间 = os . time()
			找到地图 = false
		elseif 判断_选择地图界面() then
			--显示('选择地图')
			if not 初始化页面 then
				if 判断_洛兰之森() then
					初始化页面 = true
				else
					滑动(302 , 256 , 302 , 549 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				end
				
			else
				if 入口_无法地带() then
					随机延时(500 , 600)
					if 判断_已选中无法地带() then
						入口_区域移动()
					end
					查找开始时间 = os . time()
					找到地图 = true
					上次城镇点击时间 = 当前时间 -- 更新最后点击时间
				else
					if not 找到地图 then
						if os . time() - 查找开始时间 > 20 then
							return
						end
					end
					滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {100 , 200})
					随机延时(500 , 1500)
				end
				
			end
			
		elseif 判断_选择地下城界面() then
			--显示('在选择地下城界面')
			随机延时(500 , 1000)
			按下点击({70 , 189 , 170 , 217})--勇士
			随机延时(800 , 1000)
			按下点击({567 , 264 , 710 , 316}) -- 阿登高地
			
		elseif 判断_可开燃烧交易界面() then
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			if 判断_选择副本判断疲劳值为0() or 判断_副本不可入场() then
				
				显示('不可入场')
				清屏(true)
				
				return
			end
			同步副本2燃烧疲劳开关状态()
			随机延时(300 , 500)
			
			按下点击({1060 , 612 , 1179 , 660}) -- 开始战斗
			等待燃烧界面加载 = true
		elseif 判断_加载界面() then
			刚从副本中出来 = false
		elseif 判断_副本中() then
			自动刷图()
			刚从副本中出来 = true
			等待燃烧界面加载 = false
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			随机延时(500 , 1000)
		elseif 判断_返回城镇() then
			
			功能_返回城镇界面获取疲劳值()
			
			if 当前疲劳值 <= 19 then
				
				入口_返回城镇()
				
				随机延时(1000 , 1500)
				while true do
					sleep(1000)
					
					if 判断_城镇界面() then
						
						return
						
					elseif 判断_副本中() then
						自动刷图()
						break
					elseif 判断_返回城镇() then
						break
					else
						清屏(true)
					end
				end
			else
				if 入口_再次挑战() then
					
				else
					入口_返回城镇()
				end
			end
		elseif 入口_确认(true) then
			
		else
			
			显示('副本2_阿登高地勇士:游戏加载中...')
			-- 安全清屏机制：避免界面异常时卡死
			if not 等待燃烧界面加载 then
				if 清屏(true) then
					print('执行清屏操作，避免界面卡死')
					随机延时(500 , 1000)
					
				end
			end
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
		end
	end
	
end

function 副本2_不刷疲劳()
	显示('副本2不刷')
	清屏(true)
end
function 副本2_普鲁兹发电站()
	print('没有疲劳了结束副本2科鲁兹发电站普通')
	初始化城镇界面()
	local 上次城镇判断时间 = 0 -- 新增：记录上次判断城镇的时间
	local 上次城镇点击时间 = 0 -- 新增：记录上次城镇点击的时间
	local 刚从副本中出来 = false
	local 初始化页面 = false
	local 等待燃烧界面加载 = false
	local 选择副本难度 = false
	local 清屏时间 = os.time()
	local 判断普通 = false
	local 判断冒险 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		显示('副本2 普鲁兹发电站_普通 运行中...')
		local 当前时间 = os.time()
		
		--检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			上次城镇判断时间 = 当前时间 -- 更新判断时间
			if 判断_疲劳值为0() then
				print('没有疲劳了结束副本2科鲁兹发电站普通')
				return
			end
			-- 检查是否已经过了足够的间隔时间（10秒）
			if 当前时间 - 上次城镇点击时间 > 20 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				随机延时(500 , 1000)
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()--冒险奖励
				随机延时(2000 , 2500)
				刚从副本中出来 = false
				
			else
				显示(' 副本2_普鲁兹发电站_普通:城镇界面操作冷却中，跳过...')
				随机延时(1000 , 2000)
			end
			查找开始时间 = os . time()
			找到地图 = false
			清屏时间 = os.time()
		elseif 判断_选择地图界面() then
			if not 初始化页面 then
				if 判断_洛兰之森() then
					初始化页面 = true
					
				else
					滑动(302 , 256 , 302 , 549 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				end
				
			else
				if 入口_能源中心() then
					随机延时(300 , 400)
					if 判断_已选中能源中心() then
						入口_区域移动()
					end
					等待燃烧界面加载 = false
					查找开始时间 = os . time()
					找到地图 = true
					上次城镇点击时间 = 当前时间 -- 更新最后点击时间
					
				else
					
					if not 找到地图 then
						
						if os . time() - 查找开始时间 > 20 then
							
							return
						end
					end
					滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {100 , 200})
					随机延时(500 , 1500)
				end
				
			end
			清屏时间 = os.time()
		elseif 判断_普鲁兹发电站选择难度界面() then
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			
			if not 选择副本难度 then
				
				if 同步副本难度选择功能() == '普通' then
					
					按下点击({875 , 587 , 925 , 606})
					随机延时(500 , 600)
					按下点击({875 , 587 , 925 , 606})
					选择副本难度 = true
					判断普通 = true
				else
					按下点击({1046 , 567 , 1116 , 609})
					随机延时(500 , 600)
					按下点击({1046 , 567 , 1116 , 609})
					选择副本难度 = true
					判断冒险 = true
				end
			else
				随机延时(500 , 600)
				if 判断_副本不可入场() then
					
					if not 判断冒险 then
						按下点击({1046 , 567 , 1116 , 609})
						随机延时(500 , 600)
						按下点击({1046 , 567 , 1116 , 609})
						判断冒险 = true
					elseif not 判断普通 then
						按下点击({875 , 587 , 925 , 606})
						随机延时(500 , 600)
						按下点击({875 , 587 , 925 , 606})
						判断普通 = true
					else
						清屏(true)
						return
						
					end
					
				else
					同步燃烧疲劳开关状态()
					随机延时(400 , 500)
					按下点击({1034 , 656 , 1197 , 697})--入场
				end
			end
			
			等待燃烧界面加载 = true
			清屏时间 = os.time()
		elseif 判断_能源中心选择地下城界面() then
			--显示('在选择地下城界面')
			
			按下点击({367 , 338 , 464 , 519}) --普鲁兹发电站
			随机延时(900 , 1000)
			清屏时间 = os.time()
		elseif 判断_加载界面() then
			刚从副本中出来 = false
			清屏时间 = os.time()
		elseif 判断_副本中() then
			
			自动刷图()
			刚从副本中出来 = true
			等待燃烧界面加载 = false
			清屏时间 = os.time()
		elseif 判断_装备修理()then
			
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			随机延时(500 , 1000)
			清屏时间 = os.time()
		elseif 判断_返回城镇() then
			
			local 当前疲劳值 = 功能_获取疲劳值()
			功能_返回城镇界面获取疲劳值()
			
			if 判断_副本中疲劳值为0() then
				显示('疲劳值为0，返回城镇')
				入口_返回城镇()
				
			else
				
				if 入口_再次挑战() then
					
				else
					入口_返回城镇()
					
				end
			end
			清屏时间 = os.time()
		elseif 入口_确认(true) then
			清屏时间 = os.time()
		else
			
			显示(' 副本2_普鲁兹发电站_普通:游戏加载中...')
			
			-- 安全清屏机制：避免界面异常时卡死
			if not 等待燃烧界面加载 or os.time() - 清屏时间 > 4 then
				if 清屏(true) then
					print('执行清屏操作，避免界面卡死')
					随机延时(500 , 1000)
					清屏时间 = os.time()
				end
			end
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
		end
	end
	
end
function 副本2_格兰迪发电站()
	显示('执行副本2格兰迪发电站')
	初始化城镇界面()
	local 上次城镇判断时间 = 0 -- 新增：记录上次判断城镇的时间
	local 上次城镇点击时间 = 0 -- 新增：记录上次城镇点击的时间
	local 刚从副本中出来 = false
	local 清屏时间 = os.time()
	local 等待燃烧界面加载 = false
	local 选择副本难度 = false
	local 判断普通 = false
	local 判断冒险 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		local 当前时间 = os.time()
		
		-- 检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			随机延时(300 , 400)
			上次城镇判断时间 = 当前时间 -- 更新判断时间
			if 判断_疲劳值为0() then
				
				return
			end
			-- 检查是否已经过了足够的间隔时间（10秒）
			if 当前时间 - 上次城镇点击时间 > 20 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				随机延时(500 , 1000)
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				--随机延时(2000 , 3000)
				刚从副本中出来 = false
				
			else
				显示('副本2格兰迪发电站:城镇界面操作冷却中，跳过...')
				--随机延时(1000 , 2000)
			end
			查找开始时间 = os . time()
			找到地图 = false
			清屏时间 = os.time()
		elseif 判断_选择地图界面() then
			-- 更新最后操作时间
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			
			if 入口_能源中心() then
				随机延时(300 , 400)
				if 判断_已选中能源中心() then
					入口_区域移动()
				end
				等待燃烧界面加载 = true
				查找开始时间 = os . time()
				找到地图 = true
			else
				if not 找到地图 then
					if os . time() - 查找开始时间 > 20 then
						
						return
					end
				end
				滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {100 , 200})
				随机延时(500 , 1500)
			end
			清屏时间 = os.time()
		elseif 判断_普鲁兹发电站选择难度界面() then
			if not 选择副本难度 then
				
				if 同步副本难度选择功能() == '普通' then
					
					按下点击({875 , 587 , 925 , 606})
					随机延时(500 , 600)
					按下点击({875 , 587 , 925 , 606})
					选择副本难度 = true
					判断普通 = true
				else
					按下点击({1046 , 567 , 1116 , 609})
					随机延时(500 , 600)
					按下点击({1046 , 567 , 1116 , 609})
					选择副本难度 = true
					判断冒险 = true
				end
			else
				随机延时(500 , 600)
				if 判断_副本不可入场() then
					
					if not 判断冒险 then
						按下点击({1046 , 567 , 1116 , 609})
						随机延时(500 , 600)
						按下点击({1046 , 567 , 1116 , 609})
						判断冒险 = true
					elseif not 判断普通 then
						按下点击({875 , 587 , 925 , 606})
						随机延时(500 , 600)
						按下点击({875 , 587 , 925 , 606})
						判断普通 = true
					else
						清屏(true)
						return
						
					end
					
				else
					同步燃烧疲劳开关状态()
					随机延时(400 , 500)
					按下点击({1034 , 656 , 1197 , 697})--入场
				end
			end
			
			等待燃烧界面加载 = true
			清屏时间 = os.time()
		elseif 判断_加载界面() then
			刚从副本中出来 = false
			清屏时间 = os.time()
		elseif 判断_副本中() then
			等待燃烧界面加载 = false
			自动刷图()
			刚从副本中出来 = true
			清屏时间 = os.time()
		elseif 判断_能源中心选择地下城界面() then
			
			按下点击({1020 , 194 , 1096 , 337}) --格兰迪发电站
			清屏时间 = os.time()
			随机延时(900 , 1000)
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			清屏时间 = os.time()
		elseif 判断_返回城镇() then
			
			功能_返回城镇界面获取疲劳值()
			
			if 功能_获取疲劳值() == 0 then
				
				入口_返回城镇()
				
			else
				if 入口_再次挑战() then
					
				else
					
					入口_返回城镇()
					
				end
				
			end
			清屏时间 = os.time()
		elseif 入口_确认(true) then
			清屏时间 = os.time()
		else
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			显示('副本2格兰迪发电站 :游戏加载中...')
			if not 等待燃烧界面加载 or os.time() - 清屏时间 > 4 then
				if 清屏(true) then
					print('执行清屏操作，避免界面卡死')
					随机延时(500 , 1000)
					清屏时间 = os.time()
				end
			end
			
		end
	end
	
end

function 副本2_裂变试验室()
	显示('执行副本2温特科技园')
	初始化城镇界面()
	local 上次城镇判断时间 = 0 -- 新增：记录上次判断城镇的时间
	local 上次城镇点击时间 = 0 -- 新增：记录上次城镇点击的时间
	local 刚从副本中出来 = false
	local 清屏时间 = os.time()
	local 初始化页面 = false
	local 等待燃烧界面加载 = false
	local 选择副本难度 = false
	
	local 判断普通 = false
	local 判断冒险 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		local 当前时间 = os.time()
		
		--检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			上次城镇判断时间 = 当前时间 -- 更新判断时间
			if 判断_疲劳值为0() then
				
				return
			end
			-- 检查是否已经过了足够的间隔时间（10秒）
			if 当前时间 - 上次城镇点击时间 > 20 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				随机延时(500 , 1000)
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				随机延时(2000 , 3000)
				刚从副本中出来 = false
				
			else
				显示('温特科技园_普通:城镇界面操作冷却中，跳过...')
				随机延时(1000 , 2000)
			end
			清屏时间 = os.time()
			查找开始时间 = os . time()
			找到地图 = false
		elseif 判断_选择地图界面() then
			if not 初始化页面 then
				if 判断_洛兰之森() then
					初始化页面 = true
				else
					滑动(302 , 256 , 302 , 549 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				end
				
			else
				if 入口_温特科学园() then
					随机延时(300 , 400)
					if 判断_已选中温特科技园() then
						入口_区域移动()
						随机延时(2000 , 3500)
					end
					等待燃烧界面加载 = false
					查找开始时间 = os . time()
					找到地图 = true
				else
					if not 找到地图 then
						if os . time() - 查找开始时间 > 20 then
							
							return
						end
					end
					滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {100 , 200})
					随机延时(500 , 1000)
				end
				
			end
			
			上次城镇点击时间 = 0
			清屏时间 = os.time()
		elseif 判断_可开燃烧交易界面() then
			上次城镇点击时间 = 当前时间 -- 更新最后点击时间
			if not 选择副本难度 then
				
				if 同步副本难度选择功能() == '普通' then
					
					按下点击({875 , 587 , 925 , 606})
					随机延时(500 , 600)
					按下点击({875 , 587 , 925 , 606})
					选择副本难度 = true
					判断普通 = true
				else
					按下点击({1046 , 567 , 1116 , 609})
					随机延时(500 , 600)
					按下点击({1046 , 567 , 1116 , 609})
					选择副本难度 = true
					判断冒险 = true
				end
			else
				随机延时(500 , 600)
				if 判断_副本不可入场() then
					
					if not 判断冒险 then
						按下点击({1046 , 567 , 1116 , 609})
						随机延时(500 , 600)
						按下点击({1046 , 567 , 1116 , 609})
						判断冒险 = true
					elseif not 判断普通 then
						按下点击({875 , 587 , 925 , 606})
						随机延时(500 , 600)
						按下点击({875 , 587 , 925 , 606})
						判断普通 = true
					else
						
						清屏(true)
						
						return
						
					end
					
				else
					同步副本2燃烧疲劳开关状态()
					随机延时(400 , 500)
					按下点击({1034 , 656 , 1197 , 697})--入场
				end
			end
			
			等待燃烧界面加载 = true
			清屏时间 = os.time()
		elseif 判断_副本中() then
			等待燃烧界面加载 = false
			自动刷图()
			刚从副本中出来 = true
			清屏时间 = os.time()
		elseif 判断_能源中心选择地下城界面() then
			--显示('在选择地下城界面')
			
			按下点击({825 , 344 , 906 , 506}) --裂变实验室普通
			--入口_温特科学园随机副本()
			随机延时(900 , 1000)
			清屏时间 = os.time()
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			随机延时(500 , 1000)
			清屏时间 = os.time()
		elseif 判断_加载界面() then
			刚从副本中出来 = false
			清屏时间 = os.time()
		elseif 判断_返回城镇() then
			local 当前疲劳值 = 功能_获取疲劳值()
			
			功能_返回城镇界面获取疲劳值()
			
			if 判断_副本中疲劳值为0() then
				入口_返回城镇()
				
			else
				if 入口_再次挑战() then
					----一直在再次挑战界面()
					
				else
					
					入口_返回城镇()
					
				end
				
			end
			清屏时间 = os.time()
		elseif 入口_确认(true) then
			清屏时间 = os.time()
		else
			
			显示('温特科技园_普通:游戏加载中...')
			冷却时间 = os.time() -- 更新冷却时间
			-- 安全清屏机制：避免界面异常时卡死
			if not 等待燃烧界面加载 or os.time() - 清屏时间 > 4 then
				if 清屏(true) then
					print('执行清屏操作，避免界面卡死')
					随机延时(500 , 1000)
					清屏时间 = os.time()
				end
			end
		end
	end
	
end

------80级副本
function 副本2_闪光密林()
	显示('执行副本2闪光密林')
	local 刚从副本中出来 = false
	local 清屏时间 = 0
	local 冷却点击 = 0
	local 等待燃烧界面加载 = false
	local 选择副本难度 = false
	local 选择闪光密林 = false
	
	local 判断普通不可入场 = false
	local 判断冒险不可入场 = false
	local 判断王者不可入场 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		--检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			上次城镇判断时间 = 当前时间 -- 更新判断时间
			清屏时间 = os.time()
			local 当前时间 = os.time()
			local 当前疲劳值 = 功能_获取疲劳值()
			if 判断_疲劳值为0() then
				
				return
			end
			-- 检查是否已经过了足够的间隔时间（10秒）
			if 当前时间 - 冷却点击 > 20 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				刚从副本中出来 = false
				
			else
				显示('闪光密林:城镇界面操作冷却中，跳过...')
				--随机延时(1000 , 2000)
			end
			查找开始时间 = os . time()
			找到地图 = false
			
		elseif 判断_选择地图界面() then
			if 入口_侵蚀之地() then
				随机延时(500 , 600)
				if 判断_已侵蚀之地() then
					入口_区域移动()
					冷却点击 = os.time()
					随机延时(2000 , 3500)
				end
				
				查找开始时间 = os . time()
				等待燃烧界面加载 = true
				找到地图 = true
			else
				if not 找到地图 then
					if os . time() - 查找开始时间 > 20 then
						return
					end
				end
				滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				随机延时(500 , 800)
			end
			
			清屏时间 = os.time()
			
			--[===[elseif 判断_可开燃烧交易界面() then
			]===]
		elseif 判断_选择地下城界面() then
			清屏时间 = os.time()
			if not 判断_入场_搬砖图() then
				
				按下点击({591 , 319 , 634 , 384})
				
			else
				
				if not 选择闪光密林 then
					随机延时(800 , 1000)
					按下点击({28 , 92 , 271 , 171})
					随机延时(300 , 400)
					按下点击({28 , 92 , 271 , 171})
					选择闪光密林 = true
				end
				
				上次城镇点击时间 = 当前时间 -- 更新最后点击时间
				
				if not 选择副本难度 then
					
					if 同步副本难度选择功能() == '王者' then
						print(同步副本难度选择功能())
						按下点击({1119 , 585 , 1168 , 610})
						随机延时(600 , 800)
						按下点击({1119 , 585 , 1168 , 610})
						判断王者不可入场 = true
						选择副本难度 = true
						
					elseif 同步副本难度选择功能() == '冒险' then
						print(同步副本难度选择功能())
						按下点击({1003 , 587 , 1043 , 608})
						随机延时(600 , 800)
						按下点击({1003 , 587 , 1043 , 608})
						判断冒险不可入场 = true
						选择副本难度 = true
						
					elseif 同步副本难度选择功能() == '普通' then
						print(同步副本难度选择功能())
						按下点击({876 , 577 , 925 , 617})
						随机延时(600 , 800)
						按下点击({876 , 577 , 925 , 617})
						判断普通不可入场 = true
						选择副本难度 = true
						
					else
						选择副本难度 = true
						
					end
				else
					随机延时(500 , 600)
					if 判断_不可入场80级副本() then
						
						if not 判断王者不可入场 then
							按下点击({1119 , 585 , 1168 , 610})
							随机延时(600 , 800)
							按下点击({1119 , 585 , 1168 , 610})
							判断王者不可入场 = true
						elseif not 判断冒险不可入场 then
							按下点击({1003 , 587 , 1043 , 608})
							随机延时(600 , 800)
							按下点击({1003 , 587 , 1043 , 608})
							判断冒险不可入场 = true
						elseif not 判断普通不可入场 then
							按下点击({876 , 577 , 925 , 617})
							随机延时(600 , 800)
							按下点击({876 , 577 , 925 , 617})
							判断普通不可入场 = true
						else
							显示('所有难度都不可入场')
							清屏(true)
							return
						end
						
					else
						同步副本2燃烧疲劳开关状态()
						随机延时(400 , 500)
						按下点击({1034 , 656 , 1197 , 697})--入场
					end
				end
				冷却点击 = 0
				清屏时间 = os.time()
				等待燃烧界面加载 = false
				
			end
			
		elseif 判断_副本中() then
			等待燃烧界面加载 = false
			自动刷图()
			刚从副本中出来 = true
			冷却点击 = 0
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			--
			清屏时间 = os.time()
		elseif 判断_加载界面() then
			
			清屏时间 = os.time()
		elseif 判断_返回城镇() then
			
			功能_返回城镇界面获取疲劳值()
			
			local 当前疲劳值 = 功能_获取疲劳值()
			
			if 判断_副本中疲劳值为0() then
				
				if 入口_返回城镇() then
					
				end
				
			else
				
				if 入口_再次挑战() then
					
				else
					入口_返回城镇()
				end
				
			end
			清屏时间 = os.time()
			
		elseif 入口_确认(true) then
			清屏时间 = os.time()
		else
			
			冷却点击 = os.time()
			显示('闪光密林:游戏加载中...')
			
			if not 等待燃烧界面加载 or os.time() - 清屏时间 >= 5 then
				if 清屏() then
					print('执行清屏操作，避免界面卡死')
					清屏时间 = os.time()
				end
			end
		end
		::闪光密林标签::
	end
	
end
function 副本2_苍穹之海()
	显示('执行副本2 苍穹之海')
	local 刚从副本中出来 = false
	local 清屏时间 = 0
	local 冷却点击 = 0
	local 等待燃烧界面加载 = false
	local 选择副本难度 = false
	local 苍穹之海 = false
	
	local 判断普通不可入场 = false
	local 判断冒险不可入场 = false
	local 判断王者不可入场 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		local 当前时间 = os.time()
		
		--检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			
			local 当前疲劳值 = 功能_获取疲劳值()
			if 当前疲劳值 == (0 or 1) then
				
				return
			end
			-- 检查是否已经过了足够的间隔时间（10秒）
			if os.time() - 冷却点击 > 20 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				刚从副本中出来 = false
				
			else
				显示('苍穹之海:城镇界面操作冷却中，跳过...')
				--随机延时(1000 , 2000)
			end
			查找开始时间 = os . time()
			找到地图 = false
			清屏时间 = os.time()
		elseif 判断_选择地图界面() then
			
			if 判断_已侵蚀之地() then
				
				入口_区域移动()
				随机延时(2000 , 3500)
				查找开始时间 = os . time()
				冷却点击 = os.time()
				等待燃烧界面加载 = true
				找到地图 = true
			elseif not 入口_侵蚀之地() then
				
				if not 找到地图 then
					if os . time() - 查找开始时间 > 20 then
						return
					end
				end
				滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				随机延时(500 , 800)
				
			end
			
			清屏时间 = os.time()
			
			--[===[elseif 判断_可开燃烧交易界面() then
			]===]
		elseif 判断_选择地下城界面() then
			
			if not 判断_入场_搬砖图() then
				
				按下点击({591 , 319 , 634 , 384})
				
			else
				
				if not 苍穹之海 then
					随机延时(800 , 1000)
					按下点击({21 , 208 , 268 , 283})
					随机延时(300 , 400)
					按下点击({21 , 208 , 268 , 283})
					苍穹之海 = true
					
				end
				
				if not 选择副本难度 then
					
					if 同步副本难度选择功能() == '王者' then
						print(同步副本难度选择功能())
						按下点击({1119 , 585 , 1168 , 610})
						随机延时(600 , 800)
						按下点击({1119 , 585 , 1168 , 610})
						判断王者不可入场 = true
						选择副本难度 = true
						
					elseif 同步副本难度选择功能() == '冒险' then
						print(同步副本难度选择功能())
						按下点击({1003 , 587 , 1043 , 608})
						随机延时(600 , 800)
						按下点击({1003 , 587 , 1043 , 608})
						判断冒险不可入场 = true
						选择副本难度 = true
						
					elseif 同步副本难度选择功能() == '普通' then
						print(同步副本难度选择功能())
						按下点击({876 , 577 , 925 , 617})
						随机延时(600 , 800)
						按下点击({876 , 577 , 925 , 617})
						判断普通不可入场 = true
						选择副本难度 = true
						
					else
						选择副本难度 = true
						
					end
				else
					随机延时(500 , 600)
					if 判断_不可入场80级副本() then
						
						if not 判断王者不可入场 then
							按下点击({1119 , 585 , 1168 , 610})
							随机延时(600 , 800)
							按下点击({1119 , 585 , 1168 , 610})
							判断王者不可入场 = true
						elseif not 判断冒险不可入场 then
							按下点击({1003 , 587 , 1043 , 608})
							随机延时(600 , 800)
							按下点击({1003 , 587 , 1043 , 608})
							判断冒险不可入场 = true
						elseif not 判断普通不可入场 then
							按下点击({876 , 577 , 925 , 617})
							随机延时(600 , 800)
							按下点击({876 , 577 , 925 , 617})
							判断普通不可入场 = true
						else
							显示('所有难度都不可入场')
							清屏(true)
							return
						end
						
					else
						同步副本2燃烧疲劳开关状态()
						随机延时(400 , 500)
						按下点击({1034 , 656 , 1197 , 697})--入场
					end
				end
				清屏时间 = os.time()
				等待燃烧界面加载 = false
				
			end
			
		elseif 判断_副本中() then
			等待燃烧界面加载 = false
			
			自动刷图()
			刚从副本中出来 = true
			冷却点击 = 0
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			--
			清屏时间 = os.time()
		elseif 判断_加载界面() then
			
			清屏时间 = os.time()
		elseif 判断_返回城镇() then
			
			功能_返回城镇界面获取疲劳值()
			local 当前疲劳值 = 功能_获取疲劳值()
			
			if 判断_疲劳不足() or 当前疲劳值 == (0 or 1) then
				
				if 入口_返回城镇() then
					
				end
				
			else
				if 入口_再次挑战() then
					
				else
					入口_返回城镇()
				end
				
			end
			清屏时间 = os.time()
			
		elseif 入口_确认(true) then
			清屏时间 = os.time()
		else
			
			冷却点击 = os.time()
			显示('苍穹之海:游戏加载中...')
			
			if not 等待燃烧界面加载 or os.time() - 清屏时间 > 4 then
				if 清屏() then
					print('执行清屏操作，避免界面卡死')
					清屏时间 = os.time()
				end
			end
		end
		
	end
	
end

function 副本2_炽热草原()
	显示('执行副本2 炽热草原')
	local 刚从副本中出来 = false
	local 清屏时间 = 0
	local 冷却点击 = 0
	local 等待燃烧界面加载 = false
	local 选择副本难度 = false
	local 炽热草原 = false
	
	local 判断普通不可入场 = false
	local 判断冒险不可入场 = false
	local 判断王者不可入场 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		local 当前时间 = os.time()
		
		--检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			
			local 当前疲劳值 = 功能_获取疲劳值()
			if 当前疲劳值 == (0 or 1) then
				
				return
			end
			-- 检查是否已经过了足够的间隔时间（10秒）
			if os.time() - 冷却点击 > 20 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1500)
				入口_冒险奖励()
				刚从副本中出来 = false
				
			else
				显示('炽热草原:城镇界面操作冷却中，跳过...')
				--随机延时(1000 , 2000)
			end
			查找开始时间 = os . time()
			找到地图 = false
			清屏时间 = os.time()
		elseif 判断_选择地图界面() then
			
			if 判断_已侵蚀之地() then
				
				入口_区域移动()
				随机延时(2000 , 3500)
				查找开始时间 = os . time()
				冷却点击 = os.time()
				等待燃烧界面加载 = true
				找到地图 = true
			elseif not 入口_侵蚀之地() then
				
				if not 找到地图 then
					if os . time() - 查找开始时间 > 20 then
						return
					end
				end
				滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				随机延时(500 , 800)
				
			end
			
			清屏时间 = os.time()
			
			--[===[elseif 判断_可开燃烧交易界面() then
			]===]
		elseif 判断_选择地下城界面() then
			
			if not 判断_入场_搬砖图() then
				
				按下点击({591 , 319 , 634 , 384})
				
			else
				
				if not 炽热草原 then
					随机延时(800 , 1000)
					按下点击({20 , 309 , 274 , 393})
					随机延时(300 , 400)
					按下点击({20 , 309 , 274 , 393})
					炽热草原 = true
					
				end
				
				if not 选择副本难度 then
					
					if 同步副本难度选择功能() == '王者' then
						print(同步副本难度选择功能())
						按下点击({1119 , 585 , 1168 , 610})
						随机延时(600 , 800)
						按下点击({1119 , 585 , 1168 , 610})
						判断王者不可入场 = true
						选择副本难度 = true
						
					elseif 同步副本难度选择功能() == '冒险' then
						print(同步副本难度选择功能())
						按下点击({1003 , 587 , 1043 , 608})
						随机延时(600 , 800)
						按下点击({1003 , 587 , 1043 , 608})
						判断冒险不可入场 = true
						选择副本难度 = true
						
					elseif 同步副本难度选择功能() == '普通' then
						print(同步副本难度选择功能())
						按下点击({876 , 577 , 925 , 617})
						随机延时(600 , 800)
						按下点击({876 , 577 , 925 , 617})
						判断普通不可入场 = true
						选择副本难度 = true
						
					else
						选择副本难度 = true
						
					end
				else
					随机延时(500 , 600)
					if 判断_不可入场80级副本() then
						
						if not 判断王者不可入场 then
							按下点击({1119 , 585 , 1168 , 610})
							随机延时(600 , 800)
							按下点击({1119 , 585 , 1168 , 610})
							判断王者不可入场 = true
						elseif not 判断冒险不可入场 then
							按下点击({1003 , 587 , 1043 , 608})
							随机延时(600 , 800)
							按下点击({1003 , 587 , 1043 , 608})
							判断冒险不可入场 = true
						elseif not 判断普通不可入场 then
							按下点击({876 , 577 , 925 , 617})
							随机延时(600 , 800)
							按下点击({876 , 577 , 925 , 617})
							判断普通不可入场 = true
						else
							显示('所有难度都不可入场')
							清屏(true)
							return
						end
						
					else
						同步副本2燃烧疲劳开关状态()
						随机延时(400 , 500)
						按下点击({1034 , 656 , 1197 , 697})--入场
					end
				end
				清屏时间 = os.time()
				等待燃烧界面加载 = false
				
			end
			
		elseif 判断_副本中() then
			等待燃烧界面加载 = false
			自动刷图()
			刚从副本中出来 = true
			冷却点击 = 0
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			
			清屏时间 = os.time()
		elseif 判断_加载界面() then
			
			清屏时间 = os.time()
		elseif 判断_返回城镇() then
			
			功能_返回城镇界面获取疲劳值()
			
			local 当前疲劳值 = 功能_获取疲劳值()
			if 判断_疲劳不足() or 当前疲劳值 == (0 or 1) then
				
				if 入口_返回城镇() then
					
				end
				
			else
				if 入口_再次挑战() then
					
				else
					入口_返回城镇()
				end
				
			end
			清屏时间 = os.time()
			
		elseif 入口_确认(true) then
			清屏时间 = os.time()
		else
			
			冷却点击 = os.time()
			显示('炽热草原:游戏加载中...')
			
			if not 等待燃烧界面加载 or os.time() - 清屏时间 > 4 then
				if 清屏() then
					print('执行清屏操作，避免界面卡死')
					清屏时间 = os.time()
				end
			end
		end
		
	end
	
end

function 副本2_复苏之灾()
	
	显示('执行副本2复苏之灾')
	
	local 刚从副本中出来 = false
	local 清屏时间 = 0
	local 冷却点击 = 0
	
	local 等待燃烧界面加载 = false
	local 选择副本难度 = false
	local 选择复苏之灾 = false
	
	local 判断普通不可入场 = false
	local 判断冒险不可入场 = false
	local 判断王者不可入场 = false
	local 首次运行 = true
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		--检测游戏是否闪退
		if 快速检测游戏状态() then
			显示('检测到游戏闪退，自动重启流程从头开始')
			-- 递归调用自己，从头开始
			
		end
		
		if 判断_城镇界面() then
			--显示('在城镇界面')
			local 当前时间 = os.time()
			local 当前疲劳值 = 功能_获取疲劳值()
			
			if 当前疲劳值 > 1 and 当前疲劳值 < 40 then
				显示('当前疲劳值不足40点前往闪光密林')
				return 副本2_闪光密林()
			elseif 当前疲劳值 < 1 then
				return
			end
			-- 检查是否已经过了足够的间隔时间（10秒）
			if 当前时间 - 冷却点击 > 20 or 刚从副本中出来 then
				显示('执行城镇界面操作')
				
				按下点击({1103 , 632 , 1148 , 678}) -- 点击冒险
				随机延时(800 , 1000)
				入口_冒险奖励()
				刚从副本中出来 = false
				
			else
				显示('执行副本2复苏之灾:城镇界面操作冷却中，跳过...')
				--随机延时(1000 , 2000)
			end
			查找开始时间 = os . time()
			找到地图 = false
			清屏时间 = os.time()
		elseif 判断_选择地图界面() then
			
			if 入口_侵蚀之地() then
				随机延时(500 , 600)
				if 判断_已侵蚀之地() then
					入口_区域移动()
					冷却点击 = os.time()
					随机延时(2000 , 3500)
				end
				
				查找开始时间 = os . time()
				等待燃烧界面加载 = true
				找到地图 = true
			else
				if not 找到地图 then
					if os . time() - 查找开始时间 > 20 then
						return
					end
				end
				滑动(296 , 576 , 301 , 294 , 50 , 10 , {15 , 35} , {800 , 1000} , {300 , 400})
				随机延时(300 , 600)
			end
			
			清屏时间 = os.time()
			
			--[===[elseif 判断_可开燃烧交易界面() then
			]===]
		elseif 判断_选择地下城界面() then
			
			if not 判断_入场_搬砖图() then
				
				按下点击({591 , 319 , 634 , 384})
				
			else
				
				if not 选择复苏之灾 then
					随机延时(800 , 1000)
					按下点击({24 , 422 , 273 , 501})--复苏之灾
					随机延时(300 , 400)
					按下点击({24 , 422 , 273 , 501})--复苏之灾
					选择复苏之灾 = true
					
				end
				
				if not 选择副本难度 then
					
					if 同步副本难度选择功能() == '王者' then
						print(同步副本难度选择功能())
						按下点击({1119 , 585 , 1168 , 610})
						随机延时(600 , 800)
						按下点击({1119 , 585 , 1168 , 610})
						判断王者不可入场 = true
						选择副本难度 = true
						
					elseif 同步副本难度选择功能() == '冒险' then
						print(同步副本难度选择功能())
						按下点击({1003 , 587 , 1043 , 608})
						随机延时(600 , 800)
						按下点击({1003 , 587 , 1043 , 608})
						判断冒险不可入场 = true
						选择副本难度 = true
						
					elseif 同步副本难度选择功能() == '普通' then
						print(同步副本难度选择功能())
						按下点击({876 , 577 , 925 , 617})
						随机延时(600 , 800)
						按下点击({876 , 577 , 925 , 617})
						判断普通不可入场 = true
						选择副本难度 = true
						
					else
						选择副本难度 = true
						
					end
				else
					随机延时(500 , 600)
					if 判断_不可入场80级副本() then
						
						if not 判断王者不可入场 then
							按下点击({1119 , 585 , 1168 , 610})
							随机延时(600 , 800)
							按下点击({1119 , 585 , 1168 , 610})
							判断王者不可入场 = true
						elseif not 判断冒险不可入场 then
							按下点击({1003 , 587 , 1043 , 608})
							随机延时(600 , 800)
							按下点击({1003 , 587 , 1043 , 608})
							判断冒险不可入场 = true
						elseif not 判断普通不可入场 then
							按下点击({876 , 577 , 925 , 617})
							随机延时(600 , 800)
							按下点击({876 , 577 , 925 , 617})
							判断普通不可入场 = true
						else
							显示('所有难度都不可入场')
							清屏(true)
							return
						end
						
					else
						同步副本2燃烧疲劳开关状态()
						随机延时(400 , 500)
						按下点击({1034 , 656 , 1197 , 697})--入场
					end
				end
				清屏时间 = os.time()
				等待燃烧界面加载 = false
				
			end
			冷却点击 = 0
		elseif 判断_副本中() then
			等待燃烧界面加载 = false
			自动刷图()
			刚从副本中出来 = true
			冷却点击 = 0
		elseif 判断_装备修理()then
			随机延时(800 , 1500)
			按下点击({977 , 604 , 1163 , 651})--修理
			随机延时(300 , 500)
			清屏(true)
			--
			清屏时间 = os.time()
		elseif 判断_加载界面() then
			
			清屏时间 = os.time()
		elseif 判断_返回城镇() then
			
			功能_返回城镇界面获取疲劳值()
			local 当前疲劳值 = 功能_获取疲劳值()
			if 判断_疲劳不足() or 当前疲劳值 == 0 or 当前疲劳值 < 40 then
				
				if 入口_返回城镇() then
					
				end
				
			else
				if 入口_再次挑战() then
					
				else
					入口_返回城镇()
				end
				
			end
			清屏时间 = os.time()
			
		elseif 入口_确认(true) then
			清屏时间 = os.time()
		else
			
			冷却点击 = os.time()
			显示('执行副本2复苏之灾:游戏加载中...')
			
			if not 等待燃烧界面加载 or os.time() - 清屏时间 > 4 then
				if 清屏() then
					print('执行清屏操作，避免界面卡死')
					清屏时间 = os.time()
				end
			end
		end
		::复苏之灾标签::
	end
	
end

function 副本2_搬砖图自动()
	显示('运行自动选择搬砖图')
	初始化城镇界面()
	local 获取抗魔值 = false
	
	local 主线已判断 = false
	local 运行时间 = os.time()
	local 首次运行 = true
	local 做主线 = false
	while true do
		if not 首次运行 then
			随机延时(500 , 600)
		end
		首次运行 = false
		if 快速检测游戏状态() then
			
		end
		
		if os.time() - 运行时间 > 60 then
			显示('自动选择副本超时')
			return
		end
		
		if 判断_城镇界面() then
			
			当前疲劳值 = 功能_获取疲劳值()
			if 判断_疲劳值为0() then
				return
			end
			if not 做主线 then
			   
				if 城镇主线()  then
					副本1_主线1至70()
					运行时间 = os.time()
				end
				做主线 = true
			else
				按下点击({1198 , 638 , 1233 , 676}) --背包
			end
			
		elseif 判断_打开背包界面() then
			
			if not 获取抗魔值 then
				
				local 新抗魔值 = 判断_抗魔值()
				
				if 新抗魔值 then
					if 当前角色抗魔值 == nil or 新抗魔值 > 当前角色抗魔值 then
						当前角色抗魔值 = 新抗魔值
						显示('当前角色抗魔值更新为:' .. 当前角色抗魔值)
					else
						显示('当前角色抗魔值保留更大值:' .. 当前角色抗魔值)
					end
					
					if 当前角色抗魔值 >= 61500 then
						
						初始化城镇界面()
						if 当前疲劳值 < 40 then
							return 副本2_闪光密林()
						else
							return 副本2_复苏之灾()
						end
						
					elseif 当前角色抗魔值 >= 60000 then
						
						return 副本2_闪光密林()
						
					elseif 当前角色抗魔值 >= 20500 then
						
						return 副本2_裂变试验室()
						
					elseif 当前角色抗魔值 >= 17500 then
						
						return 副本2_格兰迪发电站()
					else
						
						显示('抗魔值不足1.75万结束刷图')
						return 副本2_格兰迪发电站()
					end
					获取抗魔值 = true
				else
					显示('未能获取到当前抗魔值')
				end
				
			end
		else
			清屏(true)
		end
		
	end
	
end

